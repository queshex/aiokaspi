[project]
name = "aiokaspi"
version = "0.1.0"
description = "Async library for Kaspi Pay"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.14.3",
    "cryptography>=49.0.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.1",
    "ruff>=0.16.0",
]
